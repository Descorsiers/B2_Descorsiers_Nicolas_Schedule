{% extends 'base.html.twig' %}

{% block title %}Modifier le planning de la journée{% endblock %}

{% block body %}
    <section id="scheduleEdit">
        <h1>
            Modifier le planning de la journée
        </h1>
        {{ include('day_schedule/_form.html.twig', {'button_label': 'Modifier'}) }}
        <div class="scheduleEdit">
            <a href="{{ path('app_day_schedule_index') }}">
                Retourner à la liste
            </a>
            {{ include('day_schedule/_delete_form.html.twig') }}
        </div>
    </section>
{% endblock %}
