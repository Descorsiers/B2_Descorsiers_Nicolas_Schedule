<form method="post" action="{{ path('app_day_schedule_delete', {'id': day_schedule.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ day_schedule.id) }}">
    <button>
        Supprimer
    </button>
</form>
