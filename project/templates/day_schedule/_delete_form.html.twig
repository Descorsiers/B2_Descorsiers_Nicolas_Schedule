<form method="post" action="{{ path('app_day_schedule_delete', {'id': day_schedule.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ day_schedule.id) }}">
    <button class="btn">Delete</button>
</form>
